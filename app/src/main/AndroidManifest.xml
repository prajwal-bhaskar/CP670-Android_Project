<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:tools="http://schemas.android.com/tools">

    <application
        android:allowBackup="true"
        android:dataExtractionRules="@xml/data_extraction_rules"
        android:fullBackupContent="@xml/backup_rules"
        android:icon="@drawable/canaconnect"
        android:label="@string/app_name"
        android:roundIcon="@drawable/canaconnect"
        android:supportsRtl="true"
        android:theme="@style/Theme.Event_Management"
        tools:targetApi="31">
        <activity
            android:name=".LandingPage"
            android:exported="true"
            android:label="@string/title_activity_landing_page" >
        <intent-filter>
            <action android:name="android.intent.action.MAIN" />

            <category android:name="android.intent.category.LAUNCHER" />
        </intent-filter></activity>
        <activity
            android:name=".ui.login.LoginActivity"
            android:exported="false"
            android:label="@string/title_activity_login" />
        
        <activity
            android:name=".ui.events.EventDescriptionActivity"
            android:label="Event Description"
            android:exported="true">
        </activity>
        <activity
            android:name=".EventBookingActivity"
            android:label="Event Description"
            android:exported="true">
        </activity>

    </application>

</manifest>